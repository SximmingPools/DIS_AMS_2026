[project]
name = "dis-ams"
version = "0.1.0"
description = "DBIS Exercise Sheet 1 Python/Postgres project"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
